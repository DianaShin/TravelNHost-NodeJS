{
  "name": "travelnhost-node",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "nodemon server/server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "dotenv": "^6.1.0",
    "express": "^4.16.2",
    "mongoose": "^5.3.7"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
